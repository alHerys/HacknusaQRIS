<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light-locked to match the Compose theme, so the launch window never flashes dark. -->
    <style name="Theme.HacknusaQRIS" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/canvas</item>
    </style>
</resources>
